version: 2

models:
  - name: dim_clientes
    description: Essa é a dimensão com informações dos clientes da northwind.
    columns:
      - name: id_cliente
        description: Chave primária da dimensão de clientes.
        tests:
          - unique
          - not_null